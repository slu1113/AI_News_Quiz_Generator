# 時事命題網站開發計畫

## 工作原則

- 一次只做一個里程碑。
- 沒經過老師驗收，不開始下一個里程碑。
- 每個里程碑完成後，都要告訴老師改了什麼、要點哪裡驗收。
- 老師回覆「通過」後，才進行下一個里程碑。

## M0 骨架

### 要做的事

- 建立 `index.html`，讓老師可以直接雙擊打開網站。
- 做出主要畫面區塊：輸入新聞、選擇出題規格、題目草稿、列印區。
- 設定基本版面，確保電腦與手機不會橫向溢位，並預留 A4 列印樣式。

### 不做的事

- 不做真正的 AI 出題。
- 不做登入。
- 不做雲端保存。

### 驗收清單

- 可以直接雙擊 `index.html` 打開。
- 畫面文字都是繁體中文。
- 電腦和手機寬度下都不會左右超出畫面。
- 可以看到完整的出題流程區塊。

## M1 表單與本機保存

### 要做的事

- 建立新聞內容、媒體名稱、日期、題型、題數、難易度等表單。
- 讓老師可以先產生可編輯的示範題目草稿。
- 讓輸入內容與題目草稿暫存在同一台電腦，下次打開還看得到。

### 不做的事

- 不接真正的 AI。
- 不做跨電腦保存。
- 不做學生登入或作答。

### 驗收清單

- 沒有填新聞內容時，會用繁體中文提醒。
- 可以選擇題型、題數與難易度。
- 可以修改產生出的題目文字。
- 關掉瀏覽器再打開，同一台電腦仍看得到先前內容。

## M2 AI 出題

### 要做的事

- 讓網站把老師輸入的新聞素材送去產生題目草稿。
- 每題包含題目、答案、詳解與出處提醒。
- 所有 AI 產出的題目都標示「草稿」，並提醒老師查核。

### 不做的事

- 不把金鑰放在前端檔案。
- 不自動勾選「已查核」。
- 不逐字照抄新聞原文。

### 驗收清單

- 老師輸入新聞素材後，可以產生題目草稿。
- 產生的題目包含出處提醒與詳解。
- 畫面清楚提醒老師查核事實、時效、著作權、偏誤與答案唯一性。
- 產生失敗時會保留老師已輸入的內容。

## M3 Google 登入

### 要做的事

- 加入老師登入與登出流程。
- 讓畫面清楚顯示目前是哪位老師登入。
- 未登入時，限制只能使用不需要保存到雲端的功能。

### 不做的事

- 不做學生登入。
- 不做同校共用題庫。
- 不保存學生個人資料。

### 驗收清單

- 老師可以登入與登出。
- 登入後看得到自己的帳號資訊。
- 未登入時不會誤以為資料已保存到雲端。
- 學生沒有登入入口。

## M4 雲端紀錄

### 要做的事

- 讓老師登入後可以保存自己的出題紀錄。
- 讓老師可以讀取、編輯、刪除自己的紀錄。
- 確保每位老師只能看到自己的紀錄。

### 不做的事

- 不做學生資料保存。
- 不做同校老師共用紀錄。
- 不做成績或作答紀錄。

### 驗收清單

- 登入後可以保存一筆出題紀錄。
- 換一台電腦登入同一帳號後，可以看到自己的紀錄。
- 不同老師看不到彼此的紀錄。
- 刪除紀錄前會先確認。

## M5 部署

### 要做的事

- 把網站部署到可公開打開的網址。
- 設定正式環境使用的金鑰與登入設定。
- 做最後檢查：電腦、手機、A4 列印、錯誤提醒與安全紅線。

### 不做的事

- 不加入新功能。
- 不放入任何真實學生資料。
- 不把任何金鑰寫進會被存進 Git 的檔案。

### 驗收清單

- 老師可以用公開網址打開網站。
- AI 出題、登入、保存紀錄都能正常操作。
- 手機與電腦都能閱讀，列印成 A4 不會超出紙張。
- GitHub 上沒有 `.env`、金鑰或憑證檔。
